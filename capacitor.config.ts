import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.saytong.booking',
  appName: 'TONG BOOKING',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
